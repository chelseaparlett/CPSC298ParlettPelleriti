#Randomly Generate a Dataframe with a column, age, and a column weight. Age should be 
#rounded to the nearest integer (round()). Weight should be rounded to the nearest
#2 decimals.Generate data using something like rnorm() or runif(). If you need info
#about these functions, use ?rnorm ...etc. 

#Make a histogram of age and weight separately (using hist()), and then use plot() to
#make a scatterplot.
#-----------------------------------------------------------------------------------
#YOUR CODE HERE









#-----------------------------------------------------------------------------------
#Use the below code to load a dataset on the body temp of two beavers (for some
#reason this exists...). beaver1 has one beaver's body temp, beaver2 has the other.
#Check out https://stat.ethz.ch/R-manual/R-devel/library/datasets/html/beavers.html
#for more info about the data.

#plot out the body for each beaver over time. Use the column activ to decide whether
#days where outside activity occured (activ = 1) have different mean temps than
#days when it did not. How can you use filtering and mean() to do that? (p.s. no
#need to do a statistical test yet, just see if they're different)

#-----------------------------------------------------------------------------------
#YOUR CODE HERE
library(datasets)
summary(beaver1)
summary(beaver2)








#-----------------------------------------------------------------------------------

