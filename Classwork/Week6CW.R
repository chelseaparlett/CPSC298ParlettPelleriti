# Using ggplot2, try to recreate the graph shown
# on GitHub (mtcarsGG.png)
#-----------------------------------------------
# YOUR CODE HERE
head(mtcars)
mtcars$cyl <- factor(mtcars$cyl)
mtcars$vs <- factor(mtcars$vs)
#-----------------------------------------------
# Use ggplot2 to create your own graphs about
# the mtcars dataset. Try new geoms!
#-----------------------------------------------
# YOUR CODE HERE


#-----------------------------------------------